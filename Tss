print("=" * 60)
print("DIAGNOSE — KATEGORISCHE SPALTEN")
print("=" * 60)

# ── Ja/Nein Spalten ───────────────────────────────────────────
for col in [COL_WECHSEL, COL_FRISCH]:
    if col in result.columns:
        print(f"\n📋 {col}:")
        print(result[col].value_counts(dropna=False).to_string())

# ── Schaumpastenrezeptur ──────────────────────────────────────
if COL_SCHAUM in result.columns:
    vals = result[COL_SCHAUM].value_counts(dropna=False)
    print(f"\n🧴 Schaumpastenrezeptur ({vals.shape[0]} unique):")
    print(vals.to_string())

# ── Walzenbezeichnung ─────────────────────────────────────────
if COL_WALZE in result.columns:
    vals = result[COL_WALZE].value_counts(dropna=False)
    print(f"\n🔧 Walzenbezeichnung ({vals.shape[0]} unique):")
    print(vals.to_string())

# ── Kaschierpastenrezeptur ────────────────────────────────────
if COL_REZEPT in result.columns:
    vals = result[COL_REZEPT].value_counts(dropna=False)
    print(f"\n🎨 Kaschierpastenrezeptur ({vals.shape[0]} unique):")
    print(vals.to_string())

print("\n" + "=" * 60)
print("WICHTIG: result DataFrame nutzen (vor Pivot)")
print("weil dort noch alle Zeilen vorhanden sind")
print("=" * 60)
